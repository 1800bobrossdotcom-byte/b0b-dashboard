import SketchpadStudio from '@/components/SketchpadStudio';

export const metadata = {
  title: 'Sketchpad | 0TYPE',
  description: 'Live creative workspace - watch the team sketch and ideate',
};

export default function SketchpadPage() {
  return <SketchpadStudio />;
}
