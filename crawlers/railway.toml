[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "always"
healthcheckPath = "/health"
healthcheckTimeout = 30

# Swarm daemon with HTTP healthcheck
# Integrates with brain for agent orchestration
